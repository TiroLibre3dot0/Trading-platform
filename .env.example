# Front-end (Vite)
# Base URL per le chiamate API dal browser
VITE_API_BASE_URL=http://localhost:4000

# Server (Express)
PORT=4000
# Origin consentita dal CORS per chiamate browser -> API
VITE_ORIGIN=http://localhost:5173

# Qlik (necessarie solo per le rotte /api/qlik/*)
QLIK_TENANT_URL=https://<tenant>.qlikcloud.com
QLIK_API_KEY=replace_me
